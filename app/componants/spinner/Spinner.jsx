// import "../globals.css";
import "./Spinner.css"

const Spinner = ({ color }) => {
  return (
    <div className="container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;